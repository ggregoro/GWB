Server toolkit: default's CLI tools + restic (backups), Far Manager (dual-pane file manager). No firewall tool (Windows Firewall already covers it), no resource monitor (Task Manager covers it). IPBan (fail2ban equivalent) deliberately not automated - see docs/reference/ipban-manual-install.md for a manual install.
